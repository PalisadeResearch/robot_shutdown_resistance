export PROJECT_ROOT=$(expand_path .)
use flake

dotenv

# layout uv
watch_file .python-version pyproject.toml uv.lock
if [ -n "$DOCENT" ]; then
    uv sync --group docent
else
    uv sync
fi
source_env .venv/bin/activate
