.env
/logs/
!docs/inspect_logs/logs
!docs/inspect_logs_new/logs
__pycache__
build/
**/.claude/settings.local.json
.direnv
scripts

strings.go
# These are temporary files generated by the `run` script.
# gitignore'ing them so they don't get included in snapshots
# of other experiments that run in parallel.
snapshot-*.log

.cursor/